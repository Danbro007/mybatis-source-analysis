<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.danbro.mapper.TeacherMapper">

    <select id="getTeacherById2" resultMap="TeacherStudent">
        SELECT t.id as tid ,t.name as tname,s.name as sname,s.id as sid from student  s RIGHT JOIN teacher t on tid = s.tid where tid = #{id}
    </select>

    <resultMap id="TeacherStudent" type="Teacher">
        <result column="tid" property="id"/>
        <result column="tname" property="name"/>
        <collection property="students" ofType="Student">
            <result column="sid" property="id"/>
            <result column="tid" property="tid"/>
            <result column="sname" property="name"/>
        </collection>
    </resultMap>
</mapper>